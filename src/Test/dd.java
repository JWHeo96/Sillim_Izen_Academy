package Test;

public class dd {

	/* 문제> 이름, 부서, 연봉, 수당 데이터를 입력 받아서, 연산 처리하여 출력해 보세요.
	 * - 상속 이용
	 * - 생성자 이용
	 * - 추상 메소드 이용
	 * 
	 * 애플리케이션 설게
	 * -----------------
	 * main class: salaryCalculator
	 * 
	 * sub class: 
	 * 1. interface: 승산
	 * - 데이터 입력: inputInterface(name, dept, salary, commition), 
	 * 			   inputName(), inputDept(), inputSalary(), input commition()
	 * - 연산 인터페이스: addInterface, public abstract void adder(int salary, int commition)
	 * 
	 * 2. sub class: InheritanceInput2
	 * - InheritanceAdder2
	 */
}
